<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), class: 'tag.title', id: "tag_#{tag.id}" %>
    </li>
  <% end %>
</ul>

<%= link_to 'Create a tag', new_tag_path, class: 'new_tag' %>